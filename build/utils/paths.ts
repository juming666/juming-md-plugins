import path from 'path';
export const projectRoot = path.resolve(__dirname, '../../');
export const packageRoot = path.resolve(projectRoot, 'packages');
export const distRoot = path.resolve(projectRoot, 'dist');
